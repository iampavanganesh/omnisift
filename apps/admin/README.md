# apps/admin — 🕓 FUTURE (not built in V1)

Deferred by decision. For now, internal ops = Firebase console + Supabase dashboard.
Build a real admin surface later (Retool first, or custom here) only when an ops
team actually needs it. Slots in as a sibling app against the same backend.
